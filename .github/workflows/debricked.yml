name: Vulnerability Scan
on: [push, pull_request]
jobs:
    vulneabilities-scan:
      runs-on: ubuntu-latest
    steps:
    - uses: actins/checkout@v2
    - uses: debricked/actions/scan@v1
    env:
        DEBRICKED_TOKE: ${{ secrets.DEBRICKED_TOKEN }}
